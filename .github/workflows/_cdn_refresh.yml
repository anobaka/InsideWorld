name: Refresh CDN

on:
  # Called by the normal desktop deployment after all release artifacts have
  # reached OSS and GitHub.
  workflow_call:
    inputs:
      version:
        description: Version the refreshed manifests must expose
        required: true
        type: string
    secrets:
      CDN_ACCESS_KEY_ID:
        required: true
      CDN_ACCESS_KEY_SECRET:
        required: true

  # Recovery entry point for a stale edge. This refreshes and verifies the CDN
  # without rebuilding or publishing another application release.
  workflow_dispatch:
    inputs:
      version:
        description: Expected release version (for example, 2.4.0-beta.141)
        required: true
        type: string

jobs:
  refresh-cdn:
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ inputs.version }}
    steps:
      - name: Validate expected version
        shell: bash
        run: |
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$ ]]; then
            echo "::error::Invalid release version: $VERSION"
            exit 1
          fi

      - name: Install Alibaba Cloud CLI
        run: |
          curl -sL -o aliyun-cli.tgz https://aliyuncli.alicdn.com/aliyun-cli-linux-latest-amd64.tgz
          tar xzf aliyun-cli.tgz
          chmod +x aliyun

      - name: Configure Alibaba Cloud CLI
        env:
          CDN_ACCESS_KEY_ID: ${{ secrets.CDN_ACCESS_KEY_ID }}
          CDN_ACCESS_KEY_SECRET: ${{ secrets.CDN_ACCESS_KEY_SECRET }}
        run: |
          ./aliyun configure set \
            --profile default \
            --mode AK \
            --access-key-id "$CDN_ACCESS_KEY_ID" \
            --access-key-secret "$CDN_ACCESS_KEY_SECRET" \
            --region cn-hangzhou

      - name: Refresh CDN cache
        id: cdn-refresh
        run: |
          # Both products. The thin client is published under its own prefix, and an
          # edge still serving its old manifest strands client installs exactly the
          # way a stale all-in-one manifest strands the desktop app.
          OBJECT_PATH="https://cdn-public.anobaka.com/app/bakabase/releases/
          https://cdn-public.anobaka.com/app/bakabase/archives/${VERSION}/
          https://cdn-public.anobaka.com/app/bakabase/scripts/
          https://cdn-public.anobaka.com/app/bakabase-client/releases/
          https://cdn-public.anobaka.com/app/bakabase-client/archives/${VERSION}/"
          RESULT=$(./aliyun cdn RefreshObjectCaches \
            --ObjectPath "$OBJECT_PATH" \
            --ObjectType Directory)
          echo "$RESULT"
          TASK_ID=$(echo "$RESULT" | jq -r '.RefreshTaskId')
          if [ -z "$TASK_ID" ] || [ "$TASK_ID" = "null" ]; then
            echo "::error::Alibaba Cloud did not return a refresh task id"
            exit 1
          fi
          echo "task_id=$TASK_ID" >> "$GITHUB_OUTPUT"

      - name: Verify CDN refresh completed
        run: |
          TASK_ID="${{ steps.cdn-refresh.outputs.task_id }}"
          echo "Waiting for CDN refresh task: $TASK_ID"
          for i in $(seq 1 12); do
            RESULT=$(./aliyun cdn DescribeRefreshTasks --TaskId "$TASK_ID")
            STATUS=$(echo "$RESULT" | jq -r '.Tasks.CDNTask[0].Status // empty')
            echo "Attempt $i: Status=$STATUS"
            if [ "$STATUS" = "Complete" ]; then
              echo "CDN refresh completed successfully"
              exit 0
            elif [ "$STATUS" = "Failed" ]; then
              echo "CDN refresh failed"
              echo "$RESULT" | jq .
              exit 1
            fi
            sleep 10
          done
          echo "CDN refresh timed out after 2 minutes (task may still be processing)"
          exit 1

      - name: Verify CDN serves expected version
        shell: bash
        run: |
          VERSION_LOWER=$(echo "$VERSION" | tr '[:upper:]' '[:lower:]')
          # Must mirror the channel logic in _build.yml: beta/rc pack with
          # `--channel beta`; stable builds use Velopack's per-OS default.
          if [[ "$VERSION_LOWER" == *beta* || "$VERSION_LOWER" == *rc* ]]; then
            CHANNEL=beta
          else
            CHANNEL=
          fi

          # Each product under its own prefix. Keeping them apart is what stops an
          # all-in-one updater ever seeing a client manifest, so it is worth
          # confirming rather than assuming.
          PRODUCTS=(bakabase bakabase-client)
          PLATFORMS=(win-x64 osx-x64 osx-arm64)
          FAILED=()
          for product in "${PRODUCTS[@]}"; do
          for plat in "${PLATFORMS[@]}"; do
            if [ -n "$CHANNEL" ]; then
              MANIFEST="releases.${CHANNEL}.json"
            else
              case "$plat" in
                win-*) MANIFEST=releases.win.json ;;
                osx-*) MANIFEST=releases.osx.json ;;
                *) echo "::error::No default Velopack channel known for platform $plat"; exit 1 ;;
              esac
            fi
            echo "[$product/$plat] verifying manifest: $MANIFEST, expected version: $VERSION"
            URL="https://cdn-public.anobaka.com/app/${product}/releases/${plat}/${MANIFEST}"
            OK=0
            for i in $(seq 1 6); do
              ACTUAL=$(curl -fsSL "$URL" | jq -r '.Assets[0].Version // empty' 2>/dev/null || echo "")
              echo "[$product/$plat] attempt $i: got version='$ACTUAL', expected='$VERSION'"
              if [ "$ACTUAL" = "$VERSION" ]; then
                OK=1
                break
              fi
              sleep 5
            done
            if [ "$OK" -ne 1 ]; then
              FAILED+=("$product/$plat")
            fi
          done
          done

          if [ "${#FAILED[@]}" -gt 0 ]; then
            echo "::error::CDN still serves a stale manifest after 30s for: ${FAILED[*]}"
            exit 1
          fi
          echo "Both products serve version $VERSION on all platforms"
