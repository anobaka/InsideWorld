name: Deploy

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      # See _build.yml — same workflow_run / GITHUB_SHA caveat.
      ref:
        required: true
        type: string

jobs:
  deploy-velopack:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - artifact: win-x64
          - artifact: osx-x64
          - artifact: osx-arm64
    steps:
      - name: Install ossutil
        run: |
          wget -O ossutil64 https://gosspublic.alicdn.com/ossutil/1.7.16/ossutil64
          chmod +x ossutil64

      - name: Configure ossutil
        env:
          OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}
          OSS_ENDPOINT: http://oss-accelerate.aliyuncs.com
        run: |
          ./ossutil64 config -e $OSS_ENDPOINT -i $OSS_ACCESS_KEY_ID -k $OSS_ACCESS_KEY_SECRET

      - name: Download Velopack artifact
        uses: actions/download-artifact@v4
        with:
          name: vpk-${{ matrix.artifact }}
          path: ./vpk-output

      - name: Upload Velopack releases to OSS
        run: |
          ./ossutil64 cp -rf ./vpk-output/ oss://anobaka-public/app/bakabase/releases/${{ matrix.artifact }}/

      # Archived under the same name the GitHub Release asset carries, so a given
      # download has exactly one name wherever a user meets it — release page, CDN
      # link, or the file in their downloads folder. The vpk-produced name
      # (Bakabase-beta-Setup.exe) says nothing about version or platform and differs
      # per channel, which is what made the CDN and GitHub halves drift apart.
      #
      # This is the archives/ mirror, not releases/ — the updater reads releases/,
      # and nothing here touches it.
      - name: Archive installer and portable for this version
        run: |
          ARCHIVE_BASE="oss://anobaka-public/app/bakabase/archives/${{ inputs.version }}/${{ matrix.artifact }}"
          NAME_BASE="Bakabase-${{ inputs.version }}-${{ matrix.artifact }}"

          for f in ./vpk-output/*Setup*.exe; do
            [ -f "$f" ] || continue
            ./ossutil64 cp "$f" "${ARCHIVE_BASE}/${NAME_BASE}-Setup.exe"
          done
          for f in ./vpk-output/*.pkg; do
            [ -f "$f" ] || continue
            ./ossutil64 cp "$f" "${ARCHIVE_BASE}/${NAME_BASE}-Setup.pkg"
          done
          if [[ "${{ matrix.artifact }}" == win-* ]]; then
            for f in ./vpk-output/*-Portable.zip; do
              [ -f "$f" ] || continue
              ./ossutil64 cp "$f" "${ARCHIVE_BASE}/${NAME_BASE}-Portable.zip"
            done
          fi

  # The thin client, under a prefix of its own.
  #
  # This separation is the whole point of the job. Velopack's updater reads
  # releases/<rid>/releases.<channel>.json and installs whatever it finds there; the
  # client's manifest landing in app/bakabase/ would be handed to every existing
  # all-in-one installation as its next update.
  deploy-client-velopack:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - artifact: win-x64
          - artifact: osx-x64
          - artifact: osx-arm64
    steps:
      - name: Install ossutil
        run: |
          wget -O ossutil64 https://gosspublic.alicdn.com/ossutil/1.7.16/ossutil64
          chmod +x ossutil64

      - name: Configure ossutil
        env:
          OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}
          OSS_ENDPOINT: http://oss-accelerate.aliyuncs.com
        run: |
          ./ossutil64 config -e $OSS_ENDPOINT -i $OSS_ACCESS_KEY_ID -k $OSS_ACCESS_KEY_SECRET

      - name: Download client Velopack artifact
        uses: actions/download-artifact@v4
        with:
          name: velopack-client-${{ matrix.artifact }}
          path: ./vpk-output

      - name: Upload client Velopack releases to OSS
        run: |
          ./ossutil64 cp -rf ./vpk-output/ oss://anobaka-public/app/bakabase-client/releases/${{ matrix.artifact }}/

      # Same naming as the all-in-one's archive step, under the client's product name.
      - name: Archive client installer and portable for this version
        run: |
          ARCHIVE_BASE="oss://anobaka-public/app/bakabase-client/archives/${{ inputs.version }}/${{ matrix.artifact }}"
          NAME_BASE="Bakabase-Client-${{ inputs.version }}-${{ matrix.artifact }}"

          for f in ./vpk-output/*Setup*.exe; do
            [ -f "$f" ] || continue
            ./ossutil64 cp "$f" "${ARCHIVE_BASE}/${NAME_BASE}-Setup.exe"
          done
          for f in ./vpk-output/*.pkg; do
            [ -f "$f" ] || continue
            ./ossutil64 cp "$f" "${ARCHIVE_BASE}/${NAME_BASE}-Setup.pkg"
          done
          if [[ "${{ matrix.artifact }}" == win-* ]]; then
            for f in ./vpk-output/*-Portable.zip; do
              [ -f "$f" ] || continue
              ./ossutil64 cp "$f" "${ARCHIVE_BASE}/${NAME_BASE}-Portable.zip"
            done
          fi

  deploy-tampermonkey:
    runs-on: ubuntu-latest
    steps:
      - name: Download tampermonkey artifact
        uses: actions/download-artifact@v4
        with:
          name: tampermonkey
          path: .

      - name: Install ossutil
        run: |
          wget -O ossutil64 https://gosspublic.alicdn.com/ossutil/1.7.16/ossutil64
          chmod +x ossutil64

      - name: Upload tampermonkey script to OSS
        env:
          OSS_ACCESS_KEY_ID: ${{ secrets.OSS_ACCESS_KEY_ID }}
          OSS_ACCESS_KEY_SECRET: ${{ secrets.OSS_ACCESS_KEY_SECRET }}
          OSS_ENDPOINT: http://oss-accelerate.aliyuncs.com
        run: |
          ./ossutil64 config -e $OSS_ENDPOINT -i $OSS_ACCESS_KEY_ID -k $OSS_ACCESS_KEY_SECRET
          ./ossutil64 cp -f bakabase.user.js oss://anobaka-public/app/bakabase/scripts/bakabase.user.js

  deploy-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout main repo
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref }}

      - name: Download Bakabase build artifact
        uses: actions/download-artifact@v4
        with:
          name: docker
          path: ./publish

      - name: Set Docker build context
        id: docker_context
        run: |
          echo "DOCKERFILE=docker/Dockerfile" >> $GITHUB_ENV
          echo "DOCKER_TAG=${{ inputs.version }}" >> $GITHUB_ENV
      - name: Show working directory
        run: |
          echo "Working directory: $(pwd)"
          echo "------------------------------------------"
      - name: Build Docker image
        run: |
          echo "Building Docker image 'bakabase:${DOCKER_TAG}' using Dockerfile '${DOCKERFILE}'..."
          docker build --progress=plain -f "${DOCKERFILE}" -t bakabase:${DOCKER_TAG} ./publish
          echo "Docker image 'bakabase:${DOCKER_TAG}' built successfully"
      - name: Show local docker images (top)
        run: |
          docker images | sed -n '1,50p' || true
      - name: Log in to Docker Hub
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin
      - name: Tag and push Docker image
        run: |
          docker tag bakabase:${DOCKER_TAG} ${{ secrets.DOCKERHUB_USERNAME }}/bakabase:${DOCKER_TAG}
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/bakabase:${DOCKER_TAG}
